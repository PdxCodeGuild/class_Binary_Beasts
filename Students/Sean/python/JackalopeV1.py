def reproduce(n):
    population = []
    while len(population) < 1000:
        age(population, 1)
        for range(4,8) in population:
            population += 2
        
def age(l, i):
    for i in l:
        [x+i]
    return l

reproduce(2)